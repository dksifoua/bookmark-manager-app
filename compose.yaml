services:
  postgres:
    image: postgres:latest
    container_name: database
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
      POSTGRES_DB: bookmark
    ports:
      - "5432:5432"